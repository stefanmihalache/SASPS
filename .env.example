# Environment variables template
# Copy this to .env and adjust as needed

# Service Configuration
SERVICE_NAME=no-caching
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=user
DB_PASSWORD=pass
DB_NAME=testdb

# Redis Configuration (not needed for no-caching)
REDIS_HOST=localhost
REDIS_PORT=6379

# Write-Behind Configuration (only for write-behind service)
WRITE_BEHIND_INTERVAL=5000

